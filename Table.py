class Table:
    def __init__():
        pass
    
    def add_row(set, item, price, range1, range2):
        """ 
        Add row template:
| {set} | ###################################### |    {stock}    |   {price}    |  {range1}-{range2} | # We need to figure out how to do items still
+-----+----------------------------------------+----------+----------+----------+  
        """
        if len(set) >= 3:
            pass # Make it ask again but I have not implemented that yet. 
        if len(item) >= 40:
            pass # Make it 
        pass

